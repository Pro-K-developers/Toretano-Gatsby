import React from "react"
import contactStyles from "./contact.module.css"

const Contact = () => {
  return <div className={contactStyles.container} id="contact">Contact</div>
}

export default Contact
